apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata:
  name: promote-to-qa
  title: Promote to QA
  description: Tag the main branch for QA deployment
spec:
  owner: platform-team
  type: automation
  parameters:
    - title: Promote Details
      required: [repoUrl, tagName]
      properties:
        repoUrl:
          type: string
        tagName:
          type: string
  steps:
    - id: promote
      name: Promote to QA
      action: custom:deploy-to-qa
      input:
        repoUrl: ${{ parameters.repoUrl }}
        baseBranch: main
        tagName: ${{ parameters.tagName }}
